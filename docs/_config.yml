title: Flowdrom Documentation
description: Transaction Timing Diagram Generator
theme: minima

# Use CommonMark (GitHub-flavored Markdown) instead of Kramdown
markdown: CommonMarkGhPages

plugins:
  - jekyll-feed
  - jekyll-sitemap

# Syntax highlighting (Rouge is still used under the hood)
highlighter: rouge

# Rouge settings
rouge:
  theme: base16   # safest bet (consistent with GitHub)
  # Alternatives: monokai, colorful, tulip, base16

